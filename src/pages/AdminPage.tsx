export function AdminPage() {
  return (
    <main className="app-page">
      <section className="page-shell empty-state">
        <span className="eyebrow">管理后台</span>
        <h1>首版先保留最小入口</h1>
        <p>当前管理员能力主要依赖 Supabase 后台处理举报、封禁与数据查看，后续再补完整 UI。</p>
      </section>
    </main>
  );
}
